let express = require('express');
let app = express();
let http = require('http').Server(app);
let jiffServer = require('{{{JIFF_PATH}}}/lib/jiff-server');
let jiffServerInstance = new jiffServer(http, { logs: true });

{{{BIG_NUMBER}}}

// Serve static files.
// Configure App
app.use('/demos', express.static('{{{JIFF_PATH}}}/demos'));
app.use('/dist', express.static('{{{JIFF_PATH}}}/dist'));
app.use('/lib/ext', express.static('{{{JIFF_PATH}}}/lib/ext'));

http.listen(8080, function () {
  console.log('listening on *:8080');
});